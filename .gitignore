__pycache__/
*.pyc
.DS_Store

# Claude Code session worktrees live here; the launch config and agents next
# to them are tracked on purpose, so every worktree gets them.
.claude/*
!.claude/launch.json
!.claude/agents/
